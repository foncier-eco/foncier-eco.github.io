image: registry.gitlab.com/pages/hugo:latest

variables:
  GIT_SUBMODULE_STRATEGY: recursive

test:
  script:
  - hugo
  except:
  - master

pages:
  working-directory: Collectif-Foncier
  script:
  - hugo --themesDir ../..
  artifacts:
    paths:
    - public
  only:
  - master
